export interface TrailDot {
  x: number;
  y: number;
  id: number;
}

export interface GuestbookEntry {
  name: string;
  message: string;
  date: string;
}

export interface Position {
  x: number;
  y: number;
}